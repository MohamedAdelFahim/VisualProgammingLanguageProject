using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ValueEndpoint : MonoBehaviour
{
    public enum Type
    {
        input1,
        input2
    }

    // Start is called before the first frame update
    public Node node;
    public ValueStartpoint startpoint;
    public Type type;

    void Start()
    {

    }

    // Update is called once per frame
    void Update()
    {

    }
}
